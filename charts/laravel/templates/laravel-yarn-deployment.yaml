apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c -f docker-compose.yml
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: laravel-yarn
  name: laravel-yarn
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: laravel-yarn
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -c -f docker-compose.yml
        kompose.version: 1.26.1 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/sail: "true"
        io.kompose.service: laravel-yarn
    spec:
      containers:
        - args:
            - yarn
            - run
            - dev
          env:
            - name: LARAVEL_SAIL
              value: "1"
            - name: WWWUSER
            - name: XDEBUG_CONFIG
              value: client_host=host.docker.internal
            - name: XDEBUG_MODE
              value: "off"
          image: sail-8.1/app
          name: laravel.yarn
          ports:
            - containerPort: 5173
          resources: {}
          volumeMounts:
            - mountPath: /var/www/html
              name: laravel-yarn-claim0
      restartPolicy: Always
      volumes:
        - name: laravel-yarn-claim0
          persistentVolumeClaim:
            claimName: laravel-yarn-claim0
status: {}
